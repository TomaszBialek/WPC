{
  "name": "app-ui",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx webpack --mode production",
    "release": "aws s3 sync ./dist${APP_BUCKET}/ --acl public-read",
    "deploy": "npm run build && npm run release"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.24.4",
    "webpack-cli": "^4.5.0"
  },
  "dependencies": {
    "aws-amplify": "^3.3.23",
    "aws-sdk": "^2.859.0"
  }
}
